<h3>Изменить параграф</h3>
<br>
<%= form_for :admabout, url: admabout_path(@admabout), method: :put do |f| %>
	<p>
		<%= f.label :text %><br>
		<%= f.text_area :text %>
	<p>
	<p>
		<%= f.submit %>
	</p>
<% end %>
<%= link_to 'Back', admabouts_path %><br>
<%= link_to 'Admin area', admin_index_path %>
